1
connect to my database
mysql -u root -p
create database miaosha

CREATE TABLE user (
  id int(11) NOT NULL,
  name varchar(24) DEFAULT NULL,
  PRIMARY KEY (id)
);

before can run the app
connect to redis in terminal: redis-server


2
CREATE TABLE `miaosha_user` (
  `id` bigint(20) NOT NULL COMMENT '用户ID，手机号码',
  `nickname` varchar(255) NOT NULL,
  `password` varchar(32) DEFAULT NULL COMMENT 'MD5(MD5(pass明文+固定salt) + salt)',
  `salt` varchar(10) DEFAULT NULL,
  `head` varchar(128) DEFAULT NULL COMMENT '头像，云存储的ID',
  `register_date` datetime DEFAULT NULL COMMENT '注册时间',
  `last_login_date` datetime DEFAULT NULL COMMENT '上蔟登录时间',
  `login_count` int(11) DEFAULT '0' COMMENT '登录次数',
  PRIMARY KEY (`id`)
)

input password: 123456, different than db pass
INSERT INTO miaosha_user (id, nickname, password, salt)
VALUES (16607350735, 'john', 'b7797cce01b4b131b433b6acf4add449', '1a2b3c4d');


<form style="width=50%; margin=0 auto"></form?=>